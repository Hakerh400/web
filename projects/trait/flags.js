'use strict';

const assert = require('assert');

const flags = {
  UnlockLevels: 1,
  OpenLastLevel: 0,
  DisplayStrings: 0,
  Persist: 0,
  Record: 1,
  DisplayNotifiedTiles: 0,
};

module.exports = flags;