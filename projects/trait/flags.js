'use strict';

const assert = require('assert');

const flags = {
  UnlockLevels: 1,
  OpenLastLevel: 1,
  DisplayStrings: 0,
  Persist: 0,
  Record: 1,
  DisplayNotifiedTiles: 0,
  LogTileOnClick: 1,
  FastAnim: 1,
  LargeTiles: 0,
};

module.exports = flags;